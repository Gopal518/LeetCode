#include<iostream>
using namespace std;


struct linkedList
{
    int data;
    struct linkedlist *next;
};

int main(){
    linkedList node;
    head = (struct node *)malloc(sizeof(struct Node));
    
}